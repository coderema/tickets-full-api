services:
  api:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ${HOME}/assets:/root/assets
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: docker-stack_app-network
